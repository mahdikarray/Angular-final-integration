import { FoyerDirective } from './foyer.directive';

describe('FoyerDirective', () => {
  it('should create an instance', () => {
    const directive = new FoyerDirective();
    expect(directive).toBeTruthy();
  });
});
